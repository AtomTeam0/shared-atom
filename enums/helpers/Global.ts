export enum Global {
  USERID = `global:userId`,
  PERMISSION = `global:permission`,
  SKIP_PLUGINS = `global:skipPlugins`,
  DEPTH = `global:depth`,
  // for singleton:
  BLOB_CLIENT = `global:blobClient`,
  SOCKET_SERVER = `global:socketServer`,
}
